@page
@model GlamCarProject.Pages.SalePersons.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.SalePerson[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SalePerson[0].Address)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SalePerson[0].CenterNo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SalePerson[0].isSenior)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.SalePerson) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Address)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CenterNo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.isSenior)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.SalePersonId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.SalePersonId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.SalePersonId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
